{
  "name": "firefox-split-scroll",
  "version": "0.1.0",
  "description": "Firefox WebExtension for split-tab scroll continuum",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "addons-linter --enable-background-service-worker src/"
  },
  "devDependencies": {
    "addons-linter": "^8.2.0",
    "vitest": "^1.6.0",
    "web-ext": "^8.3.0"
  }
}
